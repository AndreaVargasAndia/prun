#include "CArray.h"

/*template<class T> CArray<T>::CArray(int n)
{
    p=new T[n];
    tam=n;
    //ctor
}
template<class T> T& CArray<T>::operator[](int indice)
{
    if(indice<0||indice>=tam)
    {
        indice=0;
    }
    return p[indice];
}

template<class T> CArray<T>::~CArray()
{
    delete p;
    //dtor
}
*/
